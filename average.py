# Average

def average(num_one: float, num_two:float, num_three:float):
    # compute the average
    # add the numbers
    sum = num_one + num_two + num_three
    # divide by three
    return sum / 3

result = average(93, 96, 94)
print(result)
